# JR Stack — Catálogo maestro de harnesses (embebido en el instalador).
# Fuente de verdad de qué harnesses existen, de qué tipo son, de dónde se
# obtienen, en qué modo entran y de qué dependen.
#
# Convención de modos: un harness de Lite lista [lite, full] (Full incluye a
# Lite). Un harness exclusivo de Full lista [full]. Custom matchea todos.

harnesses:
  # ── Externos (no son nuestros repos; se instalan/configuran) ──────────────
  - id: openspec
    name: OpenSpec CLI
    description: CLI de Spec-Driven Development; fuente de verdad del estado.
    type: external
    external: { method: npm, pkg: "@fission-ai/openspec" }
    install_modes: [lite, full]

  - id: engram
    name: Engram
    description: Memoria persistente local (SQLite + FTS) vía MCP.
    type: external
    external:
      method: homebrew
      pkg: gentleman-programming/tap/engram
      repo: Gentleman-Programming/engram
      mcp: { name: engram, command: engram, args: [mcp] }
    install_modes: [lite, full]

  - id: context7
    name: Context7
    description: Documentación de librerías al día (MCP remoto).
    type: external
    external:
      method: mcp
      url: "https://mcp.context7.com"
      mcp:
        name: context7
        command: npx
        args: ["-y", "--package=@upstash/context7-mcp@2.2.5", "--", "context7-mcp"]
    install_modes: [lite, full]

  # ── Configuración (bundleados en el instalador) ───────────────────────────
  - id: sdd-orchestrator
    name: SDD Orchestrator
    description: Configura el orquestador SDD con piezas componibles.
    type: config
    toggles: [tdd, engram, model-routing, delegation, governance]
    install_modes: [lite, full]
    agents: [claude, opencode]

  - id: permissions
    name: Permissions (security-first)
    description: Permisos seguros por defecto (bloquea .env, confirma git destructivo). No es opcional.
    type: config
    install_modes: [lite, full]
    agents: [claude, opencode, gemini, vscode]

  # ── Skills propias ─────────────────────────────────────────────────────────
  - id: jr-orchestrator
    name: jr-orchestrator
    description: Orquestador delgado de la fase de fundación (openspec init → kb-creator → roadmap-generator → find-skill → agent-instruction).
    type: skill
    source: { repo: JuanCruzRobledo/jr-orchestrator, ref: latest, method: clone }
    # Full-only: orquesta kb-creator/roadmap-generator/agent-instruction/find-skill,
    # todas Full. En Lite quedaría huérfano. Lite = sustrato; Full = fundación guiada.
    install_modes: [full]
    depends_on: [openspec]

  - id: kb-creator
    name: kb-creator
    description: Genera la knowledge-base canónica del dominio.
    type: skill
    source: { repo: JuanCruzRobledo/kb-creator, ref: latest, method: clone }
    install_modes: [full]

  - id: roadmap-generator
    name: roadmap-generator
    description: Genera CHANGES.md (backlog técnico) desde la KB.
    type: skill
    source: { repo: JuanCruzRobledo/roadmap-generator, ref: latest, method: clone }
    install_modes: [full]
    depends_on: [openspec, kb-creator]

  - id: agent-instruction
    name: agent-instruction
    description: Genera AGENTS.md/CLAUDE.md correcto con todas las referencias.
    type: skill
    source: { repo: JuanCruzRobledo/agent-instruction, ref: latest, method: clone }
    install_modes: [full]

  - id: skill-registry
    name: skill-registry
    description: Crea/actualiza el registro de skills del proyecto.
    type: skill
    source: { repo: JuanCruzRobledo/skill-registry, ref: latest, method: clone }
    install_modes: [full]

  # ── Slash-commands (embebidos en el binario) ─────────────────────────────
  - id: starter-add-command
    name: Starter Add Command
    description: Slash-command /jr:starter-add (Claude) / /jr-starter-add (OpenCode) — wrapper thin sobre jr-stack starter add.
    type: command
    # Scoped to the two focused P0 agents only (Claude + OpenCode).
    # Other agent variants are out of scope for C-31 (D3: focused-only).
    agents: [claude, opencode]
    # Lite + Full: the command surface is part of the minimal useful install.
    install_modes: [lite, full]

  # ── Skills de terceros (se descargan con el stack) ────────────────────────
  - id: find-skill
    name: find-skill
    description: Busca y recomienda skills relevantes (registry de Vercel).
    type: skill
    third_party: true
    best_effort: true # falla del clone no aborta el install (terceros: layout puede cambiar)
    # ID propio "find-skill"; el skill upstream se llama "find-skills" (PLURAL) y vive en un subdir.
    source: { repo: vercel-labs/skills, ref: latest, method: clone, path: skills/find-skills }
    install_modes: [full]

  - id: skill-creator
    name: skill-creator
    description: Crea nuevas skills siguiendo la spec de Agent Skills.
    type: skill
    third_party: true
    best_effort: true # falla del clone no aborta el install (terceros: layout puede cambiar)
    source: { repo: anthropics/skills, ref: latest, method: clone, path: skills/skill-creator }
    install_modes: [full]

  # ── C-30: Skills curadas del starter pack ─────────────────────────────────
  # Clasificación (C-30 design.md):
  #   PROPIAS/FORK (14): apuntan al monorepo JuanCruzRobledo/jr-skills, SIN third_party.
  #   TERCEROS   (16): third_party: true, best_effort: true, apuntan al upstream confirmado.
  # install_modes: [full] — materializadas vía `jr-stack starter add`, no por modo base.

  # ── Propias puras (8) — contenido 100% del usuario ────────────────────────
  - id: fastapi-domain-service
    name: FastAPI Domain Service
    description: Patrón de servicio de dominio con FastAPI (arquitectura por capas).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/fastapi-domain-service }
    install_modes: [full]

  - id: alembic-migrations
    name: Alembic Migrations
    description: Gestión de migraciones de base de datos con Alembic + SQLAlchemy.
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/alembic-migrations }
    install_modes: [full]

  - id: zustand-store-pattern
    name: Zustand Store Pattern
    description: Patrón de store global con Zustand para React.
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/zustand-store-pattern }
    install_modes: [full]

  - id: react19-form-pattern
    name: React 19 Form Pattern
    description: Patrón de formularios con React 19 (Server Actions + useFormState).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/react19-form-pattern }
    install_modes: [full]

  - id: dashboard-crud-page
    name: Dashboard CRUD Page
    description: Página de dashboard con operaciones CRUD completas.
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/dashboard-crud-page }
    install_modes: [full]

  - id: ws-frontend-subscription
    name: WebSocket Frontend Subscription
    description: Patrón de suscripción WebSocket en el frontend (React + reconnect).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/ws-frontend-subscription }
    install_modes: [full]

  - id: help-system-content
    name: Help System Content
    description: Patrón de contenido para sistemas de ayuda in-app.
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/help-system-content }
    install_modes: [full]

  - id: monorepo-scaffold
    name: Monorepo Scaffold
    description: Scaffold de monorepo (Turborepo / pnpm workspaces).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/monorepo-scaffold }
    install_modes: [full]

  # ── Forks expandidos (6) — upstream existe pero contenido es el del usuario ─
  - id: postgresql-table-design
    name: PostgreSQL Table Design
    description: Diseño de tablas PostgreSQL (fork expandido de wshobson/agents).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/postgresql-table-design }
    install_modes: [full]

  - id: tailwind-design-system
    name: Tailwind Design System
    description: Sistema de diseño basado en Tailwind CSS (fork expandido).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/tailwind-design-system }
    install_modes: [full]

  - id: typescript-advanced-types
    name: TypeScript Advanced Types
    description: Tipos avanzados de TypeScript — generics, utility types, branded types (fork expandido).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/typescript-advanced-types }
    install_modes: [full]

  - id: interface-design
    name: Interface Design
    description: Patrón de diseño de interfaces UI (fork expandido).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/interface-design }
    install_modes: [full]

  - id: clean-architecture
    name: Clean Architecture
    description: Arquitectura limpia por capas (fork expandido — capa de dominio + aplicación + infra).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/clean-architecture }
    install_modes: [full]

  - id: fastapi-code-review
    name: FastAPI Code Review
    description: Checklist de code review para APIs FastAPI (fork expandido de wshobson/agents).
    type: skill
    scope: starter-only
    source: { repo: JuanCruzRobledo/jr-skills, ref: latest, method: clone, path: skills/fastapi-code-review }
    install_modes: [full]

  # ── Terceros — capa base/transversal (6) ──────────────────────────────────
  - id: test-driven-development
    name: Test-Driven Development
    description: Ciclo TDD — red/green/refactor (obra/superpowers).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: obra/superpowers, ref: latest, method: clone, path: skills/test-driven-development }
    install_modes: [full]

  - id: systematic-debugging
    name: Systematic Debugging
    description: Metodología de debugging sistemático (obra/superpowers).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: obra/superpowers, ref: latest, method: clone, path: skills/systematic-debugging }
    install_modes: [full]

  - id: requesting-code-review
    name: Requesting Code Review
    description: Guía para solicitar code reviews efectivos (obra/superpowers).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: obra/superpowers, ref: latest, method: clone, path: skills/requesting-code-review }
    install_modes: [full]

  - id: receiving-code-review
    name: Receiving Code Review
    description: Guía para recibir y procesar code reviews (obra/superpowers).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: obra/superpowers, ref: latest, method: clone, path: skills/receiving-code-review }
    install_modes: [full]

  - id: code-review-excellence
    name: Code Review Excellence
    description: Excelencia en code review — cómo dar reviews de calidad (wshobson/agents).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: wshobson/agents, ref: latest, method: clone, path: skills/code-review-excellence }
    install_modes: [full]

  - id: agile-product-owner
    name: Agile Product Owner
    description: Rol y responsabilidades del Product Owner ágil (alirezarezvani/claude-skills).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: alirezarezvani/claude-skills, ref: latest, method: clone, path: skills/agile-product-owner }
    install_modes: [full]

  # ── Terceros — capa backend (6) ───────────────────────────────────────────
  - id: fastapi-templates
    name: FastAPI Templates
    description: Templates de proyecto FastAPI (wshobson/agents).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: wshobson/agents, ref: latest, method: clone, path: skills/fastapi-templates }
    install_modes: [full]

  - id: python-testing-patterns
    name: Python Testing Patterns
    description: Patrones de testing en Python con pytest (wshobson/agents).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: wshobson/agents, ref: latest, method: clone, path: skills/python-testing-patterns }
    install_modes: [full]

  - id: postgresql-optimization
    name: PostgreSQL Optimization
    description: Optimización de consultas y esquema PostgreSQL (github/awesome-copilot).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: github/awesome-copilot, ref: latest, method: clone, path: skills/postgresql-optimization }
    install_modes: [full]

  - id: api-security-best-practices
    name: API Security Best Practices
    description: Prácticas de seguridad para APIs REST (sickn33/antigravity-awesome-skills).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: sickn33/antigravity-awesome-skills, ref: latest, method: clone, path: skills/api-security-best-practices }
    install_modes: [full]

  - id: multi-stage-dockerfile
    name: Multi-Stage Dockerfile
    description: Dockerfiles multi-etapa optimizados (github/awesome-copilot).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: github/awesome-copilot, ref: latest, method: clone, path: skills/multi-stage-dockerfile }
    install_modes: [full]

  - id: redis-best-practices
    name: Redis Best Practices
    description: Mejores prácticas para Redis (Mindrally/skills — upstream confirmado, raíz del repo).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: Mindrally/skills, ref: latest, method: clone, path: redis-best-practices }
    install_modes: [full]

  - id: websocket-engineer
    name: WebSocket Engineer
    description: Ingeniería de WebSockets full-stack (Jeffallan/claude-skills — upstream confirmado v1.1.0+).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: Jeffallan/claude-skills, ref: latest, method: clone, path: skills/websocket-engineer }
    install_modes: [full]

  # ── Terceros — capa frontend/ux-ui (3) ────────────────────────────────────
  - id: vercel-react-best-practices
    name: Vercel React Best Practices
    description: Mejores prácticas de React según Vercel (vercel-labs/agent-skills).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: vercel-labs/agent-skills, ref: latest, method: clone, path: skills/vercel-react-best-practices }
    install_modes: [full]

  - id: playwright-best-practices
    name: Playwright Best Practices
    description: Mejores prácticas de testing E2E con Playwright (currents-dev/playwright-skills).
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: currents-dev/playwright-skills, ref: latest, method: clone, path: skills/playwright-best-practices }
    install_modes: [full]

  - id: pwa-development
    name: PWA Development
    description: |
      Desarrollo de Progressive Web Apps (alinaqi/claude-bootstrap — upstream confirmado).
      WATCH: usa arquitectura modular con base.md; clonar el subdir puede dejar la skill incompleta.
    type: skill
    scope: starter-only
    third_party: true
    best_effort: true
    source: { repo: alinaqi/claude-bootstrap, ref: latest, method: clone, path: skills/pwa-development }
    install_modes: [full]

# ── Starters (bundles curados de harnesses, componibles vía includes) ─────────
# C-30: composición curada. Cada starter resuelve su set total vía ResolveStarter.
# Convención de includes: starter hijo → starter padre provee los transversales.
starters:
  # base: 6 harnesses transversales compartidos por todos los starters de dominio.
  - id: base
    name: Base
    description: Transversales metodológicos compartidos por todos los starters (TDD, debugging, code review, producto ágil).
    harnesses:
      - test-driven-development
      - systematic-debugging
      - requesting-code-review
      - receiving-code-review
      - code-review-excellence
      - agile-product-owner

  # backend: base + 12 harnesses de capa backend.
  - id: backend
    name: Backend
    description: Stack de backend Python/FastAPI con arquitectura limpia, BD, seguridad y contenedores.
    includes: [base]
    harnesses:
      - clean-architecture
      - fastapi-domain-service
      - fastapi-code-review
      - redis-best-practices
      - websocket-engineer
      - alembic-migrations
      - fastapi-templates
      - python-testing-patterns
      - postgresql-table-design
      - postgresql-optimization
      - api-security-best-practices
      - multi-stage-dockerfile

  # ux-ui: base + 11 harnesses de capa frontend/UX.
  - id: ux-ui
    name: UX/UI
    description: Stack de frontend React con diseño, estado, formularios, WebSockets y testing E2E.
    includes: [base]
    harnesses:
      - vercel-react-best-practices
      - zustand-store-pattern
      - react19-form-pattern
      - dashboard-crud-page
      - ws-frontend-subscription
      - help-system-content
      - interface-design
      - pwa-development
      - typescript-advanced-types
      - tailwind-design-system
      - playwright-best-practices

  # active-ia: backend + ux-ui + monorepo-scaffold. base se deduplica automáticamente.
  - id: active-ia
    name: Active IA
    description: Stack completo para proyectos de IA activa (compone backend + ux-ui + monorepo).
    includes: [backend, ux-ui]
    harnesses:
      - monorepo-scaffold
